#include <iostream>
using namespace std;
int main () {
	int a, b, d, r;
	cin >> a >> b;
	d = a / b;
	r = a - (b * d);
	cout << d << ' ' << r << endl;
}
                                                                        